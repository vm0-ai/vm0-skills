interface:
  display_name: "Presenter - Authority Broadcast"
  short_description: "News & Public Affairs presenter direction in Authority Broadcast"
  brand_color: "#2F5BD0"
  default_prompt: "Use $presenter-authority-broadcast to choose executable Router starters, bootstrap once, replace only marked content slots, and run the deterministic review flow."
policy:
  allow_implicit_invocation: true
