# Authority Broadcast → HyperFrames Official Layout Map

This document is the submission and operational source.

The forty entries below are semantic recipes, not DOM templates. Authority Broadcast owns the fixed background, palette, typography, presenter geometry, and safety zones. The primary content visualization must be installed from the current HyperFrames Registry and preserved as an official component or block. Do not recreate a chart, comparison, process, network, map, or media system with ordinary DOM when a matching official item exists.

- Components are merged into the scene or mounted from their installed template contract.
- Blocks remain sub-compositions wired with `data-composition-src`.
- The item below is a tested starting point, not a permanent assignment. Query the current Registry in English and replace it when another official item fits the real content better.
- The PNG gallery is a rendered proof of the official item inside the Authority stage; it is not source to trace or rebuild.

## Orientation and closure

### orientation/authority-cover

- Form: Authority Cover
- Official Registry item: `titlecard-lockup`
- Type: component
- Integration: installed template/component mount
- Render proof: settled scene at 3.12s in the 40-layout validation film.

### orientation/section-pivot

- Form: Section Pivot
- Official Registry item: `titlecard-calm`
- Type: component
- Integration: installed template/component mount
- Render proof: settled scene at 7.12s in the 40-layout validation film.

### orientation/briefing-map

- Form: Briefing Map
- Official Registry item: `grid-card-assemble + line-by-line-slide` through the supplied content adapter
- Type: Authority content adapter
- Integration: bundled adapter preserving the named official behaviors
- Render proof: settled scene at 18.92s in the 40-layout validation film.

### orientation/public-action-close

- Form: Public Action Close
- Official Registry item: `cta-close + line-by-line-slide` through the supplied content adapter
- Type: Authority content adapter
- Integration: bundled adapter preserving the named official behaviors
- Render proof: settled scene at 25.12s in the 40-layout validation film.

## Text and evidence

### text/bullet-hierarchy

- Form: Bullet Hierarchy
- Official Registry item: `line-by-line-slide + inline-highlight`
- Type: component merge
- Integration: merge both installed snippets and preserve their timeline recipes
- Render proof: settled scene at 29.9s in the 40-layout validation film.

### text/action-checklist

- Form: Action Checklist
- Official Registry item: `grid-card-assemble` through the supplied content adapter
- Type: block
- Integration: sub-composition via `data-composition-src`
- Render proof: settled scene at 37.24s in the 40-layout validation film.

### text/claim-support

- Form: Claim Support
- Official Registry item: `line-by-line-slide + inline-highlight`
- Type: component merge
- Integration: merge both installed snippets and preserve their timeline recipes
- Render proof: settled scene at 42.9s in the 40-layout validation film.

### text/two-column-argument

- Form: Two Column Argument
- Official Registry item: `split-tilt-cards` through the supplied content adapter
- Type: component
- Integration: installed template/component mount
- Render proof: settled scene at 47.12s in the 40-layout validation film.

### text/three-fact-columns

- Form: Three Fact Columns
- Official Registry item: `grid-card-assemble`
- Type: component
- Integration: installed template/component mount
- Render proof: settled scene at 51.51s in the 40-layout validation film.

### text/document-excerpt

- Form: Document Excerpt
- Official Registry item: `line-by-line-slide + inline-highlight`
- Type: component merge
- Integration: merge both installed snippets and preserve their timeline recipes
- Render proof: settled scene at 56.4s in the 40-layout validation film.

### text/source-quote

- Form: Source Quote
- Official Registry item: `testimonial-proof-card`
- Type: component
- Integration: installed template/component mount
- Render proof: settled scene at 61.4s in the 40-layout validation film.

## Metrics and charts

### data/single-stat

- Form: Single Stat
- Official Registry item: `conic-progress-ring + number-pop-in` through the supplied content adapter
- Type: Authority content adapter
- Integration: bundled adapter preserving the named official behaviors
- Render proof: settled scene at 65.62s in the 40-layout validation film.

### data/kpi-row

- Form: Kpi Row
- Official Registry item: `grid-card-assemble`
- Type: component
- Integration: installed template/component mount
- Render proof: settled scene at 70.01s in the 40-layout validation film.

### data/kpi-grid

- Form: Kpi Grid
- Official Registry item: `grid-card-assemble`
- Type: component
- Integration: installed template/component mount
- Render proof: settled scene at 74.51s in the 40-layout validation film.

### data/ranked-bars

- Form: Ranked Bars
- Official Registry item: `chart-story` through the supplied content adapter
- Type: block
- Integration: sub-composition via `data-composition-src`
- Render proof: settled scene at 87.2s in the 40-layout validation film.

### data/time-series

- Form: Time Series
- Official Registry item: `mk-line-graph`
- Type: block
- Integration: sub-composition via `data-composition-src`
- Render proof: settled scene at 95.96s in the 40-layout validation film.

### data/share-ring

- Form: Share Ring
- Official Registry item: `conic-progress-ring` through the supplied content adapter
- Type: component
- Integration: installed template/component mount
- Render proof: settled scene at 100.62s in the 40-layout validation film.

### data/scenario-range

- Form: Scenario Range
- Official Registry item: `chart-story`
- Type: component
- Integration: installed template/component mount
- Render proof: settled scene at 105.4s in the 40-layout validation film.

### data/data-table

- Form: Data Table
- Official Registry item: `grid-card-assemble + inline-highlight` through the supplied content adapter
- Type: Authority content adapter
- Integration: bundled adapter preserving the named official behaviors
- Render proof: settled scene at 112.74s in the 40-layout validation film.

## Comparison and decision

### comparison/balanced-split

- Form: Balanced Split
- Official Registry item: `split-tilt-cards` through the supplied content adapter
- Type: component
- Integration: installed template/component mount
- Render proof: settled scene at 117.62s in the 40-layout validation film.

### comparison/before-after

- Form: Before After
- Official Registry item: `before-after-wipe` through the supplied content adapter
- Type: component
- Integration: installed template/component mount
- Render proof: settled scene at 121.62s in the 40-layout validation film.

### comparison/benefit-risk

- Form: Benefit Risk
- Official Registry item: `split-tilt-cards` through the supplied content adapter
- Type: component
- Integration: installed template/component mount
- Render proof: settled scene at 125.62s in the 40-layout validation film.

### comparison/option-matrix

- Form: Option Matrix
- Official Registry item: `grid-card-assemble + state-chip-rail` through the supplied content adapter
- Type: Authority content adapter
- Integration: bundled adapter preserving the named official behaviors
- Render proof: settled scene at 130.01s in the 40-layout validation film.

### comparison/threshold-decision

- Form: Threshold Decision
- Official Registry item: `state-chip-rail + number-pop-in` through the supplied content adapter
- Type: Authority content adapter
- Integration: bundled adapter preserving the named official behaviors
- Render proof: settled scene at 134.9s in the 40-layout validation film.

## Time and process

### time/ordered-steps

- Form: Ordered Steps
- Official Registry item: `tracing-beam + grid-card-assemble` through the supplied content adapter
- Type: Authority content adapter
- Integration: bundled adapter preserving the named official behaviors
- Render proof: settled scene at 139.12s in the 40-layout validation film.

### time/milestone-timeline

- Form: Milestone Timeline
- Official Registry item: `beat-timeline` through the supplied content adapter
- Type: component
- Integration: installed template/component mount
- Render proof: settled scene at 143.12s in the 40-layout validation film.

### time/phased-roadmap

- Form: Phased Roadmap
- Official Registry item: `state-chip-rail + grid-card-assemble` through the supplied content adapter
- Type: Authority content adapter
- Integration: bundled adapter preserving the named official behaviors
- Render proof: settled scene at 147.9s in the 40-layout validation film.

### time/rollout-lanes

- Form: Rollout Lanes
- Official Registry item: `beat-timeline + grid-card-assemble` through the supplied content adapter
- Type: Authority content adapter
- Integration: bundled adapter preserving the named official behaviors
- Render proof: settled scene at 152.51s in the 40-layout validation film.

### time/deadline-countdown

- Form: Deadline Countdown
- Official Registry item: `titlecard-lockup + grid-card-assemble` through the supplied content adapter
- Type: Authority content adapter
- Integration: bundled adapter preserving the named official behaviors
- Render proof: settled scene at 156.62s in the 40-layout validation film.

### time/handoff-flow

- Form: Handoff Flow
- Official Registry item: `tracing-beam` through the supplied content adapter
- Type: component
- Integration: installed template/component mount
- Render proof: settled scene at 160.62s in the 40-layout validation film.

## Systems and relationships

### system/hub-spoke

- Form: Hub Spoke
- Official Registry item: `constellation-hub`
- Type: component
- Integration: installed template/component mount
- Render proof: settled scene at 165.01s in the 40-layout validation film.

### system/dependency-network

- Form: Dependency Network
- Official Registry item: `constellation-hub + tracing-beam` through the supplied content adapter
- Type: Authority content adapter
- Integration: bundled adapter preserving the named official behaviors
- Render proof: settled scene at 169.12s in the 40-layout validation film.

### system/decision-flow

- Form: Decision Flow
- Official Registry item: `tracing-beam + state-chip-rail` through the supplied content adapter
- Type: Authority content adapter
- Integration: bundled adapter preserving the named official behaviors
- Render proof: settled scene at 173.12s in the 40-layout validation film.

### system/layered-system

- Form: Layered System
- Official Registry item: `grid-card-assemble`
- Type: component
- Integration: installed template/component mount
- Render proof: settled scene at 177.51s in the 40-layout validation film.

## Geography and media

### media/regional-map

- Form: Regional Map
- Official Registry item: `us-map`
- Type: block
- Integration: sub-composition via `data-composition-src`
- Render proof: settled scene at 187.86s in the 40-layout validation film.

### media/flow-map

- Form: Flow Map
- Official Registry item: `us-map-flow`
- Type: block
- Integration: sub-composition via `data-composition-src`
- Render proof: settled scene at 199.86s in the 40-layout validation film.

### media/media-left

- Form: Media Left
- Official Registry item: `particle-image-reveal + line-by-line-slide` through the supplied content adapter
- Type: Authority content adapter
- Integration: bundled adapter preserving the named official behaviors
- Render proof: settled scene at 205.62s in the 40-layout validation film.

### media/media-right

- Form: Media Right
- Official Registry item: `screen-flow-carousel + line-by-line-slide` through the supplied content adapter
- Type: Authority content adapter
- Integration: bundled adapter preserving the named official behaviors
- Render proof: settled scene at 210.4s in the 40-layout validation film.

### media/evidence-grid

- Form: Evidence Grid
- Official Registry item: `screen-flow-carousel + grid-card-assemble` through the supplied content adapter
- Type: Authority content adapter
- Integration: bundled adapter preserving the named official behaviors
- Render proof: settled scene at 217.74s in the 40-layout validation film.

### media/evidence-hero

- Form: Evidence Hero
- Official Registry item: `particle-image-reveal + inline-highlight` through the supplied content adapter
- Type: Authority content adapter
- Integration: bundled adapter preserving the named official behaviors
- Render proof: settled scene at 222.62s in the 40-layout validation film.

## Known exact-component gaps

- `text/document-excerpt`: the Registry has no exact institutional-document evidence component. The proof merges official `line-by-line-slide` and `inline-highlight`; the authored scene must retain document title, date, section or page, and source.
- `data/data-table`: no exact editorial data-table item was returned by the current word-tier search. `mk-specs-list` is only a row-structured starting point; use a future official table item when available.
- `data/scenario-range`: `chart-story` supplies the official chart behavior, but real lower/estimate/upper bounds still require a truthful range band built from HyperFrames atomic rules. Do not imply uncertainty with blur or random jitter.
